diff --git a/core/corr.py b/core/corr.py
index 0ab8216..8fd1071 100644
--- a/core/corr.py
+++ b/core/corr.py
@@ -36,7 +36,7 @@ class CorrBlock:
             corr = self.corr_pyramid[i]
             dx = torch.linspace(-r, r, 2*r+1, device=coords.device)
             dy = torch.linspace(-r, r, 2*r+1, device=coords.device)
-            delta = torch.stack(torch.meshgrid(dy, dx), axis=-1)
+            delta = torch.stack(torch.meshgrid(dy, dx, indexing="ij"), axis=-1)
 
             centroid_lvl = coords.reshape(batch*h1*w1, 1, 1, 2) / 2**i
             delta_lvl = delta.view(1, 2*r+1, 2*r+1, 2)
diff --git a/core/raft.py b/core/raft.py
index 652b81a..0609f20 100644
--- a/core/raft.py
+++ b/core/raft.py
@@ -9,16 +9,32 @@ from corr import CorrBlock, AlternateCorrBlock
 from utils.utils import bilinear_sampler, coords_grid, upflow8
 
 try:
-    autocast = torch.cuda.amp.autocast
-except:
-    # dummy autocast for PyTorch < 1.6
-    class autocast:
-        def __init__(self, enabled):
-            pass
-        def __enter__(self):
-            pass
-        def __exit__(self, *args):
-            pass
+    _autocast = torch.amp.autocast
+
+    def autocast(enabled):
+        return _autocast(device_type="cuda", enabled=enabled)
+
+except Exception:
+    try:
+        _autocast = torch.cuda.amp.autocast
+
+        def autocast(enabled):
+            return _autocast(enabled=enabled)
+
+    except Exception:
+        # dummy autocast for very old PyTorch
+        class _DummyAutocast:
+            def __init__(self, enabled):
+                pass
+
+            def __enter__(self):
+                return None
+
+            def __exit__(self, *args):
+                return False
+
+        def autocast(enabled):
+            return _DummyAutocast(enabled)
 
 
 class RAFT(nn.Module):
diff --git a/core/utils/utils.py b/core/utils/utils.py
index 741ccfe..9fdc698 100644
--- a/core/utils/utils.py
+++ b/core/utils/utils.py
@@ -72,7 +72,11 @@ def bilinear_sampler(img, coords, mode='bilinear', mask=False):
 
 
 def coords_grid(batch, ht, wd, device):
-    coords = torch.meshgrid(torch.arange(ht, device=device), torch.arange(wd, device=device))
+    coords = torch.meshgrid(
+        torch.arange(ht, device=device),
+        torch.arange(wd, device=device),
+        indexing="ij",
+    )
     coords = torch.stack(coords[::-1], dim=0).float()
     return coords[None].repeat(batch, 1, 1, 1)
 
